{
  "name": "typescript-jest-jenkins",
  "version": "1.0.0",
  "description": "sample app to demo javascript CICD",
  "main": "src/index.js",
  "scripts": {
    "build": "tsc",
    "test": "jest --coverage",
    "lint": "tslint -c tslint.json -p tsconfig.json",
    "clean": "rm -rf coverage types",
    "clean-full": "rm -r coverage types node_modules package-lock.json"
  },
  "author": "Lokesh Cherukuri",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "@types/node": "^11.13.4",
    "jest": "^24.7.1",
    "ts-jest": "^24.0.2",
    "tslint": "^5.16.0",
    "typescript": "^3.4.3"
  }
}
